<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="Content-Security-Policy" content="default-src 'self'; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline'; img-src 'self' data: https:;">
    <title>Matrículas | Giga-Edu</title>
    <link href="/bootstrap/css/bootstrap.min.css" rel="stylesheet">
    <script src="/sweetalert2/sweetalert2.all.min.js"></script>
    <script src="/bootstrap/js/bootstrap.bundle.min.js"></script>
</head>
<body class="bg-light">

<nav class="navbar navbar-dark bg-dark shadow-sm mb-4">
    <div class="container-fluid px-4">
        <span class="navbar-brand mb-0 h1">Giga-Edu ERP</span>
        <a href="/dashboard" class="btn btn-outline-light btn-sm">⬅ Volver al Menú Principal</a>
    </div>
</nav>

<div class="container-fluid px-4">
    <h2 class="mb-4 text-secondary">Gestión de Matrículas y Alumnos</h2>

    <div class="row">
        <div class="col-md-5">
            <div class="card shadow-sm border-0 mb-4">
                <div class="card-header bg-white p-0 border-bottom-0">
                    <ul class="nav nav-tabs nav-fill" id="myTab" role="tablist">
                        <li class="nav-item"><button class="nav-link active fw-bold" id="individual-tab" data-bs-toggle="tab" data-bs-target="#individual" type="button">Individual / Edición</button></li>
                        <li class="nav-item"><button class="nav-link fw-bold text-success" data-bs-toggle="tab" data-bs-target="#masivo" type="button">Carga Masiva (Excel)</button></li>
                    </ul>
                </div>
                
                <div class="card-body">
                    <div class="tab-content" id="myTabContent">
                        
                        <div class="tab-pane fade show active" id="individual">
                            <form action="/admin/matriculas" method="POST" id="formIndividual">
                                <input type="hidden" name="id_estudiante" id="id_estudiante">
                                
                                <h6 class="text-primary border-bottom pb-2 mb-3">Datos Personales</h6>
                                <div class="row mb-2">
                                    <div class="col-4">
                                        <select name="tipo_documento" id="tipo_documento" class="form-select form-select-sm" required>
                                            <option value="TI">TI</option>
                                            <option value="CC">CC</option>
                                            <option value="RC">RC</option>
                                            <option value="CE">CE</option>
                                        </select>
                                    </div>
                                    <div class="col-8">
                                        <input type="text" name="documento" id="documento" class="form-control form-control-sm" placeholder="No. Documento" required>
                                    </div>
                                </div>
                                <div class="row mb-2">
                                    <div class="col-6"><input type="text" name="primer_nombre" id="primer_nombre" class="form-control form-control-sm text-uppercase" placeholder="Primer Nombre" required></div>
                                    <div class="col-6"><input type="text" name="segundo_nombre" id="segundo_nombre" class="form-control form-control-sm text-uppercase" placeholder="Segundo Nombre"></div>
                                </div>
                                <div class="row mb-2">
                                    <div class="col-6"><input type="text" name="primer_apellido" id="primer_apellido" class="form-control form-control-sm text-uppercase" placeholder="Primer Apellido" required></div>
                                    <div class="col-6"><input type="text" name="segundo_apellido" id="segundo_apellido" class="form-control form-control-sm text-uppercase" placeholder="Segundo Apellido"></div>
                                </div>
                                <div class="mb-3">
                                    <label class="form-label small text-muted mb-0">Fecha de Nacimiento</label>
                                    <input type="date" name="fecha_nacimiento" id="fecha_nacimiento" class="form-control form-control-sm">
                                </div>

                                <h6 class="text-primary border-bottom pb-2 mb-3 mt-4">Datos de Contacto</h6>
                                <div class="row mb-2">
                                    <div class="col-12"><input type="text" name="direccion" id="direccion" class="form-control form-control-sm" placeholder="Dirección"></div>
                                </div>
                                <div class="row mb-2">
                                    <div class="col-6"><input type="text" name="telefono" id="telefono" class="form-control form-control-sm" placeholder="Teléfono"></div>
                                    <div class="col-6"><input type="email" name="email" id="email" class="form-control form-control-sm" placeholder="Correo"></div>
                                </div>
                                <div class="row mb-3">
                                    <div class="col-6"><input type="text" name="acudiente_nombre" id="acudiente_nombre" class="form-control form-control-sm text-uppercase" placeholder="Acudiente"></div>
                                    <div class="col-6"><input type="text" name="acudiente_telefono" id="acudiente_telefono" class="form-control form-control-sm" placeholder="Tel. Acudiente"></div>
                                </div>

                                <div class="mb-3 p-3 bg-light rounded border">
                                    <label class="form-label fw-bold">Asignar / Cambiar Grupo:</label>
                                    <select name="id_grupo" id="id_grupo" class="form-select">
                                        <option value="">(Dejar en blanco si solo actualiza datos)</option>
                                        <% grupos.forEach(g => { %> 
                                            <option value="<%= g.id %>"><%= g.grado %> - <%= g.nomenclatura %> (<%= g.jornada %>)</option> 
                                        <% }) %>
                                    </select>
                                </div>
                                <div class="d-flex gap-2">
                                    <button type="button" class="btn btn-secondary w-25" onclick="limpiarFormulario()">Limpiar</button>
                                    <button type="submit" class="btn btn-primary w-75" id="btnGuardar">Guardar</button>
                                </div>
                            </form>
                        </div>

                        <div class="tab-pane fade" id="masivo">
                            <div class="text-center mb-4 mt-3">
                                <a href="/admin/matriculas/plantilla" class="btn btn-outline-success"><span class="fs-5">📊</span> Descargar Plantilla Excel</a>
                                <p class="small text-muted mt-2">La plantilla ahora tiene menú desplegable en la Columna J.</p>
                            </div>
                            <hr>
                            <form action="/admin/matriculas/masivo" method="POST" enctype="multipart/form-data" id="formMasivo">
                                <input type="file" name="archivo_excel" class="form-control form-control-lg mb-3" accept=".xlsx" required>
                                <button type="submit" class="btn btn-success btn-lg w-100" onclick="mostrarCarga()">Subir Estudiantes</button>
                            </form>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <div class="col-md-7">
            <div class="card shadow-sm border-0 h-100">
                <div class="card-header bg-dark text-white d-flex justify-content-between align-items-center">
                    <span>Directorio de Estudiantes</span>
                    <span class="badge bg-primary rounded-pill" id="contador"><%= matriculados.length %> Registros</span>
                </div>
                
                <div class="p-2 bg-light border-bottom">
                    <input type="text" id="busqueda" class="form-control border-secondary" placeholder="🔍 Buscar por documento, nombres o apellidos (Se actualiza al escribir)..." autocomplete="off">
                </div>

                <div class="card-body p-0" style="max-height: 650px; overflow-y: auto;">
                    <table class="table table-hover mb-0" style="font-size: 0.9rem;">
                        <thead class="table-light sticky-top">
                            <tr>
                                <th>Documento</th>
                                <th>Estudiante</th>
                                <th>Grupo</th>
                                <th class="text-center">Acción</th>
                            </tr>
                        </thead>
                        <tbody id="tabla-estudiantes">
                            <% if(matriculados.length === 0) { %>
                                <tr><td colspan="4" class="text-center text-muted py-4">No hay estudiantes.</td></tr>
                            <% } else { %>
                                <% matriculados.forEach(est => { %>
                                    <tr>
                                        <td class="align-middle"><span class="badge bg-secondary"><%= est.documento %></span></td>
                                        <td class="align-middle fw-bold"><%= est.primer_apellido %> <%= est.segundo_apellido %> <%= est.primer_nombre %> <%= est.segundo_nombre %></td>
                                        <td class="align-middle text-primary">
                                            <% if(est.grado) { %> <%= est.grado %> - <%= est.nomenclatura %> <% } else { %> <span class="text-danger">Sin Grupo</span> <% } %>
                                        </td>
                                        <td class="align-middle text-center">
                                            <button class="btn btn-sm btn-outline-primary" onclick="editarEstudiante(this)" 
                                                data-id="<%= est.id_estudiante %>" data-doc="<%= est.documento %>" 
                                                data-pnom="<%= est.primer_nombre %>" data-snom="<%= est.segundo_nombre %>" 
                                                data-pape="<%= est.primer_apellido %>" data-sape="<%= est.segundo_apellido %>">
                                                ✏️ Editar
                                            </button>
                                        </td>
                                    </tr>
                                <% }) %>
                            <% } %>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>

<script src="/js/matriculas.js"></script>

</body>
</html>