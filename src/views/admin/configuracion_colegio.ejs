<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="Content-Security-Policy" content="default-src 'self'; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline'; img-src 'self' data: https:;">
    <title>Configuración Institucional | Giga-Edu</title>
    <link href="/bootstrap/css/bootstrap.min.css" rel="stylesheet">
    <script src="/sweetalert2/sweetalert2.all.min.js"></script>
</head>
<body class="bg-light">

<nav class="navbar navbar-dark bg-dark shadow-sm mb-4">
    <div class="container-fluid px-4">
        <span class="navbar-brand mb-0 h1">Giga-Edu ERP</span>
        <a href="/dashboard" class="btn btn-outline-light btn-sm">⬅ Volver al Menú Principal</a>
    </div>
</nav>

<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-10">
            <div class="card shadow-lg border-0 rounded-lg">
                <div class="card-header bg-dark text-white text-center py-3">
                    <h4 class="mb-0">⚙️ Configuración Base de la Institución</h4>
                </div>
                <div class="card-body p-4">
                    
                    <form action="/admin/configuracion" method="POST" enctype="multipart/form-data">
                        
                        <h6 class="text-primary border-bottom pb-2 mb-3">Información Legal y Académica</h6>
                        <div class="row mb-3">
                            <div class="col-md-8">
                                <label class="small fw-bold mb-1">Nombre Completo de la Institución</label>
                                <input type="text" name="nombre" class="form-control text-uppercase" value="<%= config.nombre %>" placeholder="Ej: I.E. TÉCNICA CAMPOALEGRE" required>
                            </div>
                            <div class="col-md-4">
                                <label class="small fw-bold mb-1">Resolución de Aprobación</label>
                                <input type="text" name="resolucion" class="form-control" value="<%= config.resolucion %>" placeholder="Ej: Res. 1234 de 2023">
                            </div>
                        </div>

                        <div class="row mb-4">
                            <div class="col-md-4">
                                <label class="small fw-bold mb-1">NIT</label>
                                <input type="text" name="nit" class="form-control" value="<%= config.nit %>" placeholder="Ej: 800.123.456-7" required>
                            </div>
                            <div class="col-md-4">
                                <label class="small fw-bold mb-1">Código DANE</label>
                                <input type="text" name="dane" class="form-control" value="<%= config.dane %>" placeholder="12 dígitos">
                            </div>
                            <div class="col-md-4">
                                <label class="small fw-bold mb-1">Nombre del Rector(a)</label>
                                <input type="text" name="rector_nombre" class="form-control text-uppercase" value="<%= config.rector_nombre %>" required>
                            </div>
                        </div>

                        <h6 class="text-primary border-bottom pb-2 mb-3">Sistema Institucional de Evaluación (SIEE)</h6>
                        <div class="row mb-4 bg-light p-3 rounded border">
                            <div class="col-md-6">
                                <label class="small fw-bold mb-1">Nota Mínima Posible</label>
                                <input type="number" step="0.1" name="escala_minima" class="form-control" value="<%= config.escala_minima %>" required>
                            </div>
                            <div class="col-md-6">
                                <label class="small fw-bold mb-1">Nota Máxima Posible</label>
                                <input type="number" step="0.1" name="escala_maxima" class="form-control" value="<%= config.escala_maxima %>" required>
                            </div>
                        </div>

                        <h6 class="text-primary border-bottom pb-2 mb-3">Identidad Visual (Modo Offline)</h6>
                        <div class="row mb-4">
                            <div class="col-md-6 text-center">
                                <label class="small fw-bold mb-2">Escudo Institucional (PNG/JPG)</label>
                                <input type="file" name="logo" class="form-control mb-2" accept="image/*">
                                <% if(config.url_logo) { %>
                                    <div class="p-2 border rounded bg-white mt-2">
                                        <img src="<%= config.url_logo %>" alt="Logo Actual" style="max-height: 80px; max-width: 100%; object-fit: contain;">
                                        <div class="small text-success mt-1">Logo cargado localmente</div>
                                    </div>
                                <% } %>
                            </div>
                            
                            <div class="col-md-6 text-center">
                                <label class="small fw-bold mb-2">Firma del Rector (PNG transparente)</label>
                                <input type="file" name="firma_rector" class="form-control mb-2" accept="image/*">
                                <% if(config.url_firma_rector) { %>
                                    <div class="p-2 border rounded bg-white mt-2">
                                        <img src="<%= config.url_firma_rector %>" alt="Firma Actual" style="max-height: 80px; max-width: 100%; object-fit: contain;">
                                        <div class="small text-success mt-1">Firma cargada localmente</div>
                                    </div>
                                <% } %>
                            </div>
                        </div>

                        <div class="d-grid mt-4">
                            <button type="submit" class="btn btn-dark btn-lg">💾 Guardar Configuración Institucional</button>
                        </div>
                        
                    </form>

                </div>
            </div>
        </div>
    </div>
</div>

<script>
    // Manejo de Alertas del Servidor (SweetAlert2)
    const params = new URLSearchParams(window.location.search);
    if (params.get('msg') === 'ok') Swal.fire({ title: '¡Guardado!', text: 'La configuración y archivos se guardaron en modo Offline.', icon: 'success', timer: 2500, showConfirmButton: false });
    if (params.get('msg') === 'error') Swal.fire('Error', 'Hubo un problema guardando los datos.', 'error');
</script>

</body>
</html>