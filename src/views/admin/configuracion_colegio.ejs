<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="Content-Security-Policy" content="default-src 'self'; script-src 'self' 'unsafe-inline' https://cdn.jsdelivr.net; style-src 'self' 'unsafe-inline'; img-src 'self' data: https:;">
    <title>Configuración Institucional | Giga-Edu</title>
    <link href="/bootstrap/css/bootstrap.min.css" rel="stylesheet">
    <script src="/sweetalert2/sweetalert2.all.min.js"></script>
</head>
<body class="bg-light">

<nav class="navbar navbar-dark bg-dark shadow-sm mb-4">
    <div class="container-fluid px-4">
        <span class="navbar-brand mb-0 h1">Giga-Edu ERP</span>
        <a href="/dashboard" class="btn btn-outline-light btn-sm">⬅ Volver al Menú Principal</a>
    </div>
</nav>

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card shadow-sm border-0">
                <div class="card-header bg-dark text-white text-center py-3">
                    <h4 class="mb-0">Configuración Institucional (Boletines)</h4>
                </div>
                <div class="card-body p-4">
                    
                    <form action="/admin/configuracion" method="POST" enctype="multipart/form-data">
                        <div class="row mb-3">
                            <div class="col-md-12">
                                <label class="form-label fw-bold">Nombre Oficial de la Institución</label>
                                <input type="text" name="nombre" class="form-control" placeholder="Ej: Institución Educativa Técnica de Campoalegre" value="<%= institucion.nombre || '' %>" required>
                            </div>
                        </div>

                        <div class="row mb-3">
                            <div class="col-md-6">
                                <label class="form-label fw-bold">NIT</label>
                                <input type="text" name="nit" class="form-control" placeholder="Ej: 800.123.456-7" value="<%= institucion.nit || '' %>">
                            </div>
                            <div class="col-md-6">
                                <label class="form-label fw-bold">Código DANE</label>
                                <input type="text" name="dane" class="form-control" placeholder="Ej: 141132000123" value="<%= institucion.dane || '' %>">
                            </div>
                        </div>

                        <div class="row mb-3">
                            <div class="col-md-6">
                                <label class="form-label fw-bold">Nombre del Rector(a)</label>
                                <input type="text" name="rector_nombre" class="form-control" placeholder="Nombre completo" value="<%= institucion.rector_nombre || '' %>">
                            </div>
                            <div class="col-md-6">
                                <label class="form-label fw-bold">Resolución de Aprobación</label>
                                <input type="text" name="resolucion" class="form-control" placeholder="Aprobación oficial Secretaría de Educación del Huila" value="<%= institucion.resolucion || '' %>">
                            </div>
                        </div>

                        <hr class="my-4">

                        <div class="row mb-4">
                            <div class="col-md-6 text-center">
                                <label class="form-label fw-bold d-block">Escudo / Logo (PNG/JPG)</label>
                                <% if(institucion.url_logo) { %>
                                    <img src="<%= institucion.url_logo %>" alt="Logo Actual" class="img-thumbnail mb-2" style="max-height: 100px;">
                                <% } %>
                                <input type="file" name="logo" class="form-control form-control-sm" accept="image/*">
                            </div>
                            <div class="col-md-6 text-center">
                                <label class="form-label fw-bold d-block">Firma Digitalizada Rector(a)</label>
                                <% if(institucion.url_firma_rector) { %>
                                    <img src="<%= institucion.url_firma_rector %>" alt="Firma Actual" class="img-thumbnail mb-2" style="max-height: 100px;">
                                <% } %>
                                <input type="file" name="firma_rector" class="form-control form-control-sm" accept="image/*">
                            </div>
                        </div>

                        <div class="d-grid">
                            <button type="submit" class="btn btn-success btn-lg">Guardar y Sincronizar con Firebase</button>
                        </div>
                    </form>

                </div>
            </div>
        </div>
    </div>
</div>

</body>
</html>