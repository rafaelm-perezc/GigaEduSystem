<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="Content-Security-Policy" content="default-src 'self'; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline'; img-src 'self' data: https:;">
    <title>Configuración | Giga-Edu</title>
    <link href="/bootstrap/css/bootstrap.min.css" rel="stylesheet">
    <script src="/sweetalert2/sweetalert2.all.min.js"></script>
</head>
<body class="bg-light">

<nav class="navbar navbar-dark bg-dark shadow-sm mb-4">
    <div class="container-fluid px-4"><span class="navbar-brand mb-0 h1">Giga-Edu ERP</span><a href="/dashboard" class="btn btn-outline-light btn-sm">⬅ Volver al Menú</a></div>
</nav>

<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-10">
            <div class="card shadow-lg border-0 rounded-lg mb-5">
                <div class="card-header bg-dark text-white text-center py-3"><h4 class="mb-0">⚙️ Configuración Base</h4></div>
                <div class="card-body p-4">
                    
                    <form action="/admin/configuracion" method="POST" enctype="multipart/form-data">
                        <h6 class="text-primary border-bottom pb-2 mb-3">Información Legal y Sede Única</h6>
                        <div class="row mb-3">
                            <div class="col-md-8"><label class="small fw-bold">Nombre de la Institución</label><input type="text" name="nombre" class="form-control text-uppercase" value="<%= config.nombre %>" required></div>
                            <div class="col-md-4">
                                <label class="small fw-bold">Jornada Principal (Sede Única)</label>
                                <div class="input-group">
                                    <select name="jornada_numero" id="jornada_numero" class="form-select" required>
                                        <option value="">No.</option>
                                        <option value="1" <%= config.jornada === 'Completa' ? 'selected' : '' %>>1</option>
                                        <option value="2" <%= config.jornada === 'Mañana' ? 'selected' : '' %>>2</option>
                                        <option value="3" <%= config.jornada === 'Tarde' ? 'selected' : '' %>>3</option>
                                        <option value="4" <%= config.jornada === 'Nocturna' ? 'selected' : '' %>>4</option>
                                        <option value="5" <%= config.jornada === 'Fin de semana' ? 'selected' : '' %>>5</option>
                                        <option value="6" <%= config.jornada === 'Única' ? 'selected' : '' %>>6</option>
                                    </select>
                                    <span class="input-group-text bg-white text-primary fw-bold" id="jornada_span" style="min-width: 130px;"><%= config.jornada || 'Seleccione...' %></span>
                                </div>
                            </div>
                        </div>
                        <div class="row mb-4">
                            <div class="col-md-4"><label class="small fw-bold">Ubicación (Mun, Dpto)</label><input type="text" name="ubicacion" class="form-control text-uppercase" value="<%= config.ubicacion %>"></div>
                            <div class="col-md-3"><label class="small fw-bold">NIT</label><input type="text" name="nit" class="form-control" value="<%= config.nit %>" required></div>
                            <div class="col-md-2"><label class="small fw-bold">DANE</label><input type="text" name="dane" class="form-control" value="<%= config.dane %>"></div>
                            <div class="col-md-3"><label class="small fw-bold">Resolución</label><input type="text" name="resolucion" class="form-control text-uppercase" value="<%= config.resolucion %>"></div>
                        </div>

                        <h6 class="text-primary border-bottom pb-2 mb-3">Directivos</h6>
                        <div class="row mb-4">
                            <div class="col-md-4"><label class="small fw-bold">Rector(a)</label><input type="text" name="rector_nombre" class="form-control text-uppercase" value="<%= config.rector_nombre %>" required></div>
                            <div class="col-md-4"><label class="small fw-bold">Coordinador(a) Académico</label><input type="text" name="coordinador_academico" class="form-control text-uppercase" value="<%= config.coordinador_academico %>"></div>
                            <div class="col-md-4"><label class="small fw-bold">Coordinador(a) Disciplinario</label><input type="text" name="coordinador_disciplinario" class="form-control text-uppercase" value="<%= config.coordinador_disciplinario %>"></div>
                        </div>

                        <h6 class="text-primary border-bottom pb-2 mb-3">SIEE (Evaluación)</h6>
                        <div class="row mb-4 bg-light p-3 rounded border">
                            <div class="col-md-6"><label class="small fw-bold">Nota Mínima</label><input type="number" step="0.1" name="escala_minima" class="form-control" value="<%= config.escala_minima %>" required></div>
                            <div class="col-md-6"><label class="small fw-bold">Nota Máxima</label><input type="number" step="0.1" name="escala_maxima" class="form-control" value="<%= config.escala_maxima %>" required></div>
                        </div>

                        <h6 class="text-primary border-bottom pb-2 mb-3">Identidad Visual</h6>
                        <div class="row mb-4">
                            <div class="col-md-4 text-center">
                                <label class="small fw-bold mb-2">Escudo Institucional</label>
                                <input type="file" name="logo" class="form-control mb-2" accept="image/*">
                                <% if(config.url_logo) { %><div class="p-2 border rounded bg-white mt-2"><img src="<%= config.url_logo %>" style="height: 80px; width: 100%; object-fit: contain;"></div><% } %>
                            </div>
                            <div class="col-md-4 text-center">
                                <label class="small fw-bold mb-2">Escudo Departamental</label>
                                <input type="file" name="logo_departamento" class="form-control mb-2" accept="image/*">
                                <% if(config.url_logo_departamento) { %><div class="p-2 border rounded bg-white mt-2"><img src="<%= config.url_logo_departamento %>" style="height: 80px; width: 100%; object-fit: contain;"></div><% } %>
                            </div>
                            <div class="col-md-4 text-center">
                                <label class="small fw-bold mb-2">Firma Rector(a)</label>
                                <input type="file" name="firma_rector" class="form-control mb-2" accept="image/*">
                                <% if(config.url_firma_rector) { %><div class="p-2 border rounded bg-white mt-2"><img src="<%= config.url_firma_rector %>" style="height: 80px; width: 100%; object-fit: contain;"></div><% } %>
                            </div>
                        </div>
                        <button type="submit" class="btn btn-dark btn-lg w-100">💾 Guardar Configuración</button>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

<script src="/js/configuracion.js"></script>
</body>
</html>